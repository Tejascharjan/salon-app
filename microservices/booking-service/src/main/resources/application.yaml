server:
  port: 5005

spring:
  application:
    name: booking-service

  datasource:
    url: jdbc:mysql://localhost:3306/salon_booking
    username: root
    password: tejastc
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true