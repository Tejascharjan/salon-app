server:
  port: 5003

spring:
  application:
    name: category-service

  datasource:
    url: jdbc:mysql://localhost:3306/salon_categories
    username: root
    password: tejastc
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true