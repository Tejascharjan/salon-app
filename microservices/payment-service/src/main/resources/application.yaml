server:
     port: 5006

spring:
     application:
          name: payment-service

     datasource:
          url: jdbc:mysql://localhost:3306/salon_payment?createDatabaseIfNotExist=true
          username: root
          password: tejastc
          driver-class-name: com.mysql.cj.jdbc.Driver

     jpa:
          database-platform: org.hibernate.dialect.MySQLDialect
          hibernate:
               ddl-auto: update
          show-sql: true

     rabbitmq:
       port: 5672
       host: localhost
       username: guest
       password: guest

razorpay:
     api:
          key: "rzp_test_Nf1Bxih4T5OsO8"
          secret: "igkETsSXS63m1Cy7zYLS0DLo"

stripe:
     api:
          key: your key

eureka:
     instance:
          hostname: localhost
          port: 5006
          preferIpAddress: false

     client:
          fetch-registry: true
          register-with-eureka: true
          service-url:
               defaultZone: http://localhost:8070/eureka
